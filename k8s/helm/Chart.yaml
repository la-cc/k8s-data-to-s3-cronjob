apiVersion: v2
name: pv-data-backup
version: 1.0.0
appVersion: 1.0.0
description: This Chart deploys pv-data-backup CronJob to back up data (files or directories) from pv, encrypt, push to S3 and send a webhook notification.
